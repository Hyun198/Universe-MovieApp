import React from 'react'
import { Outlet } from 'react-router-dom'
const MoviePage = () => {
    return (
        <div>
            MoviePage
            <Outlet />
        </div>

    )
}

export default MoviePage